<%= form_for @patient do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :doctor %>
  
  <%= f.collection_select(:doctor_id, @doctors, :id, :name) %>

  <%= f.submit %>
<% end %>
